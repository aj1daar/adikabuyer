spring:
  jpa:
    show-sql: false

app:
  jwt:
    secret: ${APP_JWT_SECRET:?APP_JWT_SECRET must be set in production}
  security:
    admin-password-hash: ${APP_SECURITY_ADMIN_PASSWORD_HASH:?APP_SECURITY_ADMIN_PASSWORD_HASH must be set in production}
